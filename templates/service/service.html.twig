<div class="row flex-lg-row-reverse align-items-center g-5 py-5">
	<div class="col-10 col-sm-8 col-lg-6">
		<img src="{{ asset('uploads/' ~ service.image) }}" class="d-block mx-lg-auto img-fluid" alt="{{ service.name }}" width="700" height="500" loading="lazy">
	</div>
	<div class="col-lg-6">
		<h1 class="display-5 fw-bold text-body-emphasis lh-1 mb-3">{{ service.name }}</h1>
		<p class="lead">{{ service.description }}</p>
	</div>
</div>
